# .env 템플릿 (실제 값 없음 / git에 커밋되는 파일)
# 실제 값은 절대 이 파일에 넣지 말 것. .env 에만 넣는다.
# 새 환경에 세팅할 때 이 파일을 .env 로 복사해서 값을 채운다.

TELEGRAM_API_ID=12345678
TELEGRAM_API_HASH=0123456789abcdef0123456789abcdef
OPENAI_API_KEY=sk-...
TELEGRAM_SESSION=
